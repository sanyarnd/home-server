tls:
  certificates:
    - certFile: "/certs/cert.crt"
      keyFile: "/certs/private.key"
      stores:
        - default
  stores:
    default:
      defaultCertificate:
        certFile: "/certs/cert.crt"
        keyFile: "/certs/private.key"
